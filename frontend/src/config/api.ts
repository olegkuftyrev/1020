// API configuration
// In production, VITE_API_URL can be set to point to the backend service
// If not set, defaults to relative path '/api' (works with proxy/routing)
export const API_BASE_URL = import.meta.env.VITE_API_URL || '/api'

