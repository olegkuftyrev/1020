# "No components detected" - Что делать

Эта ошибка **нормальна** для монорепозитория! DigitalOcean не может автоматически обнаружить компоненты, если в корне репозитория нет `package.json`.

## ✅ Правильное решение

### Вариант 1: CLI (Рекомендуется)

```bash
doctl apps create --spec .do/app.yaml
```

Это создаст приложение со всеми компонентами из конфигурации, пропуская автоматическое обнаружение.

### Вариант 2: Веб-интерфейс

1. Когда видите "No components detected" - **это нормально**, просто продолжайте
2. Найдите кнопку **"Edit Spec"** или **"Edit YAML"** (обычно справа вверху или в настройках)
3. Скопируйте содержимое `.do/app.yaml` из репозитория
4. Вставьте в редактор YAML
5. Сохраните

## 🔍 Почему это происходит?

- В корне репозитория нет `package.json`
- DigitalOcean пытается автоматически определить тип приложения, просматривая корень
- Для монорепозитория (backend/ + frontend/) нужно явно указывать source directories

## ✅ В конфигурации уже указано правильно:

```yaml
services:
  - name: backend
    source_dir: backend  # ✅ Без слэша!
    
static_sites:
  - name: frontend
    source_dir: frontend  # ✅ Без слэша!
```

**Важно:** `backend`, а не `/backend`!

## 📋 Чек-лист

- ✅ Repo public (у вас public) - permissions OK
- ✅ Source directory указан как `backend` (не `/backend`)
- ✅ Source directory указан как `frontend` (не `/frontend`)
- ✅ В `backend/` есть `package.json` ✅
- ✅ В `frontend/` есть `package.json` ✅
- ✅ Конфигурация в `.do/app.yaml` правильная ✅

## 🚀 После создания приложения

Не забудьте добавить secrets в Settings → App-Level Environment Variables:
- `AUTH_PASSWORD` (type: SECRET)
- `APP_KEY` (type: SECRET, минимум 32 символа)
- `WEBHOOK_SECRET` (type: SECRET, опционально)

